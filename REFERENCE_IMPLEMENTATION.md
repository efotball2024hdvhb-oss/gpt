# Reference implementation notes

The uploaded 35-image reference set was treated as the visual/interaction source of truth: left drawer, compact black chat canvas, message action menus, attachment sheet, image viewer, edit/select flows, voice screen, top playback bar and bottom composer geometry.

The footer disclaimer shown in the previous build has been removed.

Only `liquid-gooey` from Libraries.dev remains. The previous `thinking-orbs`, `voice-glow` and `metal-fx` dependencies/imports were removed.
